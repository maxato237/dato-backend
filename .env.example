FLASK_ENV=development
SECRET_KEY=change-this-secret-key-in-production-32chars+
JWT_SECRET_KEY=change-this-jwt-secret-key-in-production-32chars+
JWT_ACCESS_TOKEN_EXPIRES=3600
JWT_REFRESH_TOKEN_EXPIRES=2592000

# PostgreSQL local (développement)
DATABASE_URL=postgresql://postgres:password@localhost:5432/dato_dev

# PostgreSQL Supabase (production) — remplacer les valeurs
# DATABASE_URL=postgresql://postgres:[password]@db.[ref].supabase.co:5432/postgres

CORS_ORIGINS=http://localhost:8080,http://localhost:3000,http://10.0.2.2:8080

# Supabase Storage (stockage des images : logo, couverture, bannière)
# Laisser vide pour stocker sur le disque local (backend/uploads) à la place.
SUPABASE_URL=https://cjkzaknvcaaarsidvbjv.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET=DATO_PROFIL_PDP_PDC

# Email (optionnel, pour envoi de codes OTP en production)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_DEFAULT_SENDER=noreply@dato.app
